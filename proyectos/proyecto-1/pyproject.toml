[project]
name = "proyecto-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "requests",
    "scikit-learn",
    "tfx==1.16.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
